// NextAuth.js v5 route handler — wired up in Phase 11
// Placeholder until GitHub OAuth credentials are configured
export const GET = () => new Response('Auth not yet configured', { status: 501 });
export const POST = () => new Response('Auth not yet configured', { status: 501 });
